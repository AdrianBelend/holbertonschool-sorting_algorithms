#include "sort.h"
/**
 * quick_sort - initialize protype
 * @array: array to be sorted
 * @size: size of array
 * Description: sort array of integers in ascending order
*/
void quick_sort(int *array, size_t size)
{
	if (array == NULL || size < 2)
		return;
}